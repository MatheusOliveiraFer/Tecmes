import { Link } from 'react-router-dom';

export const Login = () => {
    return(
        <div>
            <h1>Bem vindo a página de login</h1>
            <a href="index2.html">Home</a>
        </div>
    );
}